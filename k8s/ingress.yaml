apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lead-pipeline-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: lead-pipeline-service
                port:
                  number: 80
